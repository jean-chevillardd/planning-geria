[build]
builder = "NIXPACKS"
buildCommand = "cd server && npm install --omit=dev && cd .. && npm run build"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
